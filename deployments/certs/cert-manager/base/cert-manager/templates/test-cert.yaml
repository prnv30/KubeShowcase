apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.certificate.certificateName }}
  namespace: hipstershop
spec:
  secretName: {{ .Values.certificate.secretName }}
  dnsNames:
    - 'store.hipstershop.com'
  issuerRef:
    name: selfsigned-issuer
    kind: Issuer